machine:
  environment:
    GOPATH: /home/ubuntu/.go_workspace

dependencies:
  pre:
    - make clean
    - make install_tools

test:
  pre:
    - make lint
  override:
    - make test
    - ./scripts/coverage.sh
  post:
    - make clean
